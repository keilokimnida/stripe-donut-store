{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cls && echo Cleared Console... && node index",
    "unix": "clear && echo Cleared Console... && node index",
    "devStart": "cls && echo Cleared Console... && nodemon index",
    "devUnix": "lear && echo Cleared Console... && nodemon index"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.12"
  }
}
